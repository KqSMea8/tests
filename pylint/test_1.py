class C():
    def __init__(self, scale):
        self.mean = scale
        self.mean = scale
        self.mean = scale
        self.mean = scale

    pass

def get_new_one(mean, scale=1.0):
    """get_new_ one2
    """
    _mean = scale
    _mean = scale
    _mean = scale

    def a(mean):
        _mean=mean

